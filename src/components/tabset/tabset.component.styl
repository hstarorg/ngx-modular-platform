.j-tabset {
  .nav.nav-tabs {
    li {
      min-width: 80px;
      text-align: center;
    }

    .tab-close {
      position: absolute;
      right: 2px;
      top: 2px;
      cursor: pointer;
      border-radius: 50%;

      &:hover {
        color: #c9302c;
      }
    }
  }
}